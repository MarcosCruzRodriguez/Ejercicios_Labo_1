/*
 * listado_de_creciente.c
 *
 *  Created on: 17 sep. 2022
 *      Author: rodri
 */

#include "listado_de_creciente.h"

int inicializacionArray(int arrayNumeros[], int size, int valor)
{
	int retorno = 0;

	if(arrayNumeros != NULL && size > 0)
	{
		for(int i = 0; i < size; i++)
		{
			arrayNumeros[i] = valor;
		}
		retorno = 1;
	}
	return retorno;
}

void pedirValores(int arrayNumeros[], int size, int *contadorPositivo, int *contadorNegativo)
{
	for(int i = 0; i < size; i++)
	{
		printf("\nIngrese un numero: \n");
		scanf("%d",&arrayNumeros[i]);
		if(arrayNumeros[i] > -1)
		{
			*contadorPositivo += 1;
		}
		else
		{
			*contadorNegativo += 1;
		}
	}
}

/*void listadoNumeros(int arrayNumeros[], int size, int contadorPositivo, int contadorNegativo)
{
	int auxiliarPositivo;
	int auxiliarNegativo;

	for(int i = 0; i < contadorPositivo-1; i++)
	{
		for(int j = i+1; j < contadorPositivo; j++)
		{
			if(arrayNumeros[i] > arrayNumeros[j])
			{
				auxiliarPositivo = arrayNumeros[i];
				arrayNumeros[i] = arrayNumeros[j];
				arrayNumeros[j] = auxiliarPositivo;
			}
		}
	}
	for(int i = 0; i < contadorNegativo-1; i++)
	{
		for(int j = i+1; j < contadorNegativo; j++)
		{
			if(arrayNumeros[i] < arrayNumeros[j])
			{
				auxiliarNegativo = arrayNumeros[i];
				arrayNumeros[i] = arrayNumeros[j];
				arrayNumeros[j] = auxiliarNegativo;
			}
		}
	}
}*/
void listadoNumeros(int arrayNumeros[], int size)
{

}


void imprimirLista(int arrayNumeros[], int size)
{
	printf("Listado 1: \n");
	for(int i = 0; i < size; i++)
	{
		printf("%d ,",arrayNumeros[i]);
	}
}
